import { CentredContainer } from '../containers';
import { Title } from './styles';

const MainPreloader = () => {
  return (
    <CentredContainer>
      <Title>One assistant</Title>
    </CentredContainer>
  );
};

export default MainPreloader;
